<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_steel_blue" >

    <TextView
        android:id="@+id/txtThumbImpression"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_vertical"
        android:layout_marginTop="50dp"
        android:text="@string/str_txt_thumb_impression"
        android:textColor="@color/color_white"
        android:textSize="20sp" />

    <FrameLayout
        android:id="@+id/frameLayout"
        android:layout_width="@dimen/image_view_width"
        android:layout_height="@dimen/image_view_height"
        android:layout_below="@id/txtThumbImpression"
        android:layout_centerInParent="true"
        android:layout_gravity="center_horizontal" >

        <ImageView
            android:id="@+id/imgViewBlackTrasparentBackground"
            android:layout_width="@dimen/image_view_width"
            android:layout_height="@dimen/image_view_height"
            android:src="@drawable/black_transparent" />

        <ImageView
            android:id="@+id/imgViewThumbImpressionLogin"
            android:layout_width="@dimen/image_view_width"
            android:layout_height="@dimen/image_view_height" />

        <ImageView
            android:id="@+id/imgViewRedCross"
            android:layout_width="@dimen/image_view_width"
            android:layout_height="@dimen/image_view_height"
            android:src="@drawable/red_cross_transparent"
            android:visibility="gone" />
    </FrameLayout>

    <Button
        android:id="@+id/btnLogin"
        android:layout_width="@dimen/btn_width"
        android:layout_height="@dimen/btn_height"
        android:layout_below="@id/frameLayout"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal"
        android:layout_margin="15dp"
        android:background="@drawable/rectangular_corner"
        android:text="@string/str_btn_login"
        android:textColor="@color/color_white" />

</RelativeLayout>